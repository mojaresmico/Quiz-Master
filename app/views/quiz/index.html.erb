
<div class="container-fluid"><div class="row"><div class="col-md-10 col-md-offset-1">
	<%= form_for "/submit-quiz", url: "submit-quiz"  do |f| %>
    <h1>QUIZ <h1>
        
    <table class = "table">

	<% @questions.each do |question| %>
        <tr>
            <%items = question.content%>
            <td><%= items.html_safe %></td>
            <td class ="hidden"><%= question.answer %></td>
            <td><input class="form-control" id = A<%=question.id%> name = A<%=question.id%>></td>
        </tr>
        <% end %>
    </table>
    <input type ="submit" name ="submit" id = "submit">
    <%end%>
     
</div>